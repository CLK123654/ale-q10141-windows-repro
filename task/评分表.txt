检查项：交付路径与SQLite数据库
标准答案：output/ab_attribution_review.db、output/rebuild_ab_attribution.sql以及reports下三张声明报表均存在，数据库可由SQLite读取。
关键字段：output/ab_attribution_review.db；output/rebuild_ab_attribution.sql；output/reports/cohort_metrics.csv；output/reports/sample_contamination.csv；output/reports/attribution_window.csv
容差：路径来自任务Prompt；CSV允许LF或CRLF行尾以及必要转义。
失败边界：核心文件缺失或损坏，数据库完整性结果不是ok，或交付结果并非由SQLite生成。
可观察证据：交付成员、PRAGMA integrity_check、sqlite_schema和SQLite进程退出状态。
客观硬性标准：五个路径可读，完整性结果为ok，五张派生表均存在。
阈值：三类条件全部满足。
优先级：Gate项
所属层级：L7产物结构或Schema或专业对象层

检查项：源数据保护与规则消费
标准答案：experiment_catalog、user_dimension、assignment_event、exposure_event和conversion_event与输入库逐行相同；report_meta中的报告起止时间和转化窗口等于attribution_policy.json；三张CSV按照report_layout.csv声明导出。
关键字段：五张源表；report_meta；input_data/rules/attribution_policy.json；input_data/rules/report_layout.csv
容差：源表字段和值精确比较；CSV行尾差异不计分。
失败边界：源表被改写，策略值没有进入report_meta，任一规则文件未消费，或报表字段违反布局合同。
可观察证据：输入库与交付库的有序查询、report_meta及三张CSV表头。
客观硬性标准：五张源表一致，三个策略值一致，三份布局全部落实。
阈值：全部满足。
优先级：Gate项
所属层级：L3输入数据导入与完整性层

检查项：曝光去重与污染隔离
标准答案：valid_exposure主键集合为X1、X2、X4。sample_contamination中X3为mutual_family_conflict，X5为duplicate_exposure且保留X1，X6为user_disabled，X7为bot_user，X8为outside_experiment_window。
关键字段：valid_exposure.exposure_id；sample_contamination.source_id、reason、detail；assignment_event；experiment_catalog.family_key
容差：集合按主键比较，行序不计分；原因优先级来自attribution_policy.json。
失败边界：重复会话保留错误曝光，互斥判断未使用分桶和实验族，无效记录进入有效曝光，或污染原因错配。
可观察证据：完成SQL中的窗口排序、两张派生表和sample_contamination.csv。
客观硬性标准：三个有效主键与五条污染记录逐项一致。
阈值：八条业务记录全部正确。
优先级：高权重项
所属层级：L4数据清洗与质量控制层

检查项：逐转化归因
标准答案：C1匹配X1并为attributed，延迟120分钟；C2匹配X4并为attributed，延迟40分钟；C3为blocked_by_contamination，延迟170分钟；C4匹配X1并为outside_24h_window，延迟1650分钟。
关键字段：attribution_window中的conversion_id、matched_exposure_id、experiment_id、variant、attribution_status、latency_minutes和value_cents
容差：分钟为UTC整分钟，容差为0；窗口来自attribution_policy.json。
失败边界：污染转化计入指标，跨metric_name匹配，没有选择转化前最近曝光，超窗转化标为attributed，或转化记录缺失。
可观察证据：conversion_event、valid_exposure、sample_contamination、attribution_window和对应CSV。
客观硬性标准：四个conversion_id的曝光、状态、延迟和金额逐项正确。
阈值：四行全部正确。
优先级：高权重项
所属层级：L6领域计算与专业规则层

检查项：实验分组指标
标准答案：E100/control为有效曝光1、归因转化0、收入0、转化率0.00；E100/treatment为1、1、1000、100.00；E300/treatment为1、1、0、100.00。
关键字段：cohort_metrics中的experiment_id、variant、valid_exposures、attributed_conversions、revenue_cents和conversion_rate_pct
容差：计数和美分容差为0，百分比容差为0.01；指标由valid_exposure与attribution_window复算。
失败边界：一对多连接重复计算曝光，窗口外或污染转化进入分子，收入单位错误，或转化率使用0到1口径。
可观察证据：cohort_metrics、cohort_metrics.csv及两张上游派生表。
客观硬性标准：三个业务主键的四项指标全部正确。
阈值：三行全部正确。
优先级：高权重项
所属层级：L6领域计算与专业规则层

检查项：数据库与报表对账
标准答案：三张CSV的字段、业务行和排序符合report_layout.csv，并与数据库同名派生表逐字段一致。
关键字段：input_data/rules/report_layout.csv；output/reports下三张CSV；交付数据库中的三张同名表
容差：允许必要CSV引号及LF或CRLF行尾；按source_id、conversion_id以及experiment_id加variant比较业务行。
失败边界：字段缺失或多余，业务主键重复，CSV与数据库值不同，或排序没有采用布局合同。
可观察证据：布局合同、SQLite有序查询与CSV解析结果。
客观硬性标准：三组表头、主键集合和字段值均一致。
阈值：三份报表全部闭合。
优先级：普通项
所属层级：L9跨产物一致性与可追溯层
