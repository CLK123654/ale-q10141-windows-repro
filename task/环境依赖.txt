操作系统： Windows11、macOS或主流64位Linux。

主软件使用支持窗口函数和公共表表达式的SQLite3.40或更高版本，由sqlite3命令行完成数据处理和CSV导出。PowerShell7或更高版本负责读取规则、复制源库、传入策略值并调用SQLite，无需第三方扩展。

适合当前事件批次的普通开发电脑CPU即可完成处理，无需GPU。磁盘需容纳SQLite、数据库副本和三张报表。执行账户需能读取database、rules、starter和tools，并能在input_data下创建output。入口需设置有限超时，覆盖本题批次处理并避免异常无限等待。
